{
  "name": "select2",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel ./src/index.html --open",
    "startOpen": "parcel --open",
    "build": "parcel build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0",
    "cssnano": "^5.1.12",
    "parcel": "^2.5.0",
    "postcss": "^8.4.14"
  },
  "dependencies": {
    "gh-pages": "^4.0.0",
    "serve": "^14.0.1"
  }
}
